name: Release

on:
  push:
    branches:
      - master
    tags:
      - "v*.*.*"

jobs:
  release:
    permissions:
      id-token: write
      contents: write
      packages: write
    uses: capnspacehook/go-workflows/.github/workflows/release.yml@master
