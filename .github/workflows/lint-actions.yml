name: Lint workflows

on:
  push:
    paths:
      - ".github/workflows/*"

  workflow_dispatch: {}

jobs:
  lint-workflows:
    permissions:
      contents: read
    uses: capnspacehook/go-workflows/.github/workflows/lint-actions.yml@master
